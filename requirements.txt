# ------------------------------
# Core data-science / analysis
# ------------------------------
pandas>=2.2.0
numpy>=2.0.0
scipy>=1.13.0

# HTTP helpers (useful if you call external APIs from Python)
requests>=2.32.0
aiohttp>=3.9.0
yfinance>=0.2.40

# Optional technical-analysis helpers
#   • TA-Lib (requires the C library libta-lib to be installed on the host)
ta-lib>=0.4.24
#   • Pure-Python alternative (uncomment if you prefer no C extension)
# pandas-ta>=0.3.14b0

# Optional sentiment / NLP helpers
textblob>=0.17.1
nltk>=3.8.1   # after installing, run: python -m nltk.downloader punkt stopwords wordnet

# MongoDB driver (if you ever need to talk Mongo from Python)
pymongo>=4.7.0

# Jupyter / notebooks (optional, handy for exploratory analysis)
notebook>=7.2.2
ipykernel>=6.29.0

# Testing (if you write pytest suites)
pytest>=8.2.0
pytest-cov>=5.0.0

# Code quality / formatting (optional but nice to have)
black>=24.3.0
flake8>=7.1.0
isort>=5.13.2